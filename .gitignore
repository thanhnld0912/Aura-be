node_modules/
dist/
build/
coverage/
.DS_Store
*.log

# env — never commit real values
.env
.env.*
!.env.example
!.env.local.example

# editor
.vscode/
.idea/

# misc
*.tsbuildinfo
.turbo/

# The emitted OpenAPI document. Generated on demand by `npm run openapi:emit` and
# compared against AURA-FE's vendored copy — the contract has one committed home and
# it is AURA-FE/openapi.json, not a second one here.
server/openapi.generated.json
